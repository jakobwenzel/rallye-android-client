apply plugin: 'android-library'

repositories {
    mavenCentral()
}

android {
    compileSdkVersion 19

    defaultConfig {
        minSdkVersion 10
        targetSdkVersion 17
    }
}

dependencies {
    compile 'com.android.support:support-v4:19.+'
}
